-------------------
Problem with Arrays
-------------------
- Stores elements at contiguous locations.
- Round Robin Scheduling is very hard with Arrays.

-----------------------------------------
LinkedLists - Background and Introduction
-----------------------------------------
LinkedLists like arrays are sequential data structure, but unlike Arrays elements don't
have to be at contiguous memory locations.

Every node stores the location of pointers of the next node.

